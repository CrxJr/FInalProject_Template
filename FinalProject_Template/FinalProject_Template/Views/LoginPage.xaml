<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinalProject_Template.Views.LoginPage"
             Shell.NavBarIsVisible="False"
             Shell.FlyoutBehavior="Disabled">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Pin Code" HorizontalOptions="CenterAndExpand"
                   VerticalOptions="EndAndExpand"/>
            <Entry Text="{Binding Pin}" Placeholder="Pin Key" IsPassword="True" Keyboard="Numeric"
                   VerticalOptions="Start"  HorizontalTextAlignment="Center" HorizontalOptions="CenterAndExpand" MaxLength="4"/>

            <Button Text="Login" Command="{Binding LoginCommand}"
                    HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>